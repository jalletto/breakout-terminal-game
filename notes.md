Introduction
Screen Painter is a Workbench Tool (transaction SE51) that allows developers to design screens, for example:

positioning screen fields and graphical elements on the screen
defining the flow logic, i.e. how the screen fields are initialized just before the screen is displayed, and how they are validated after the user has pressed a key or a button. This part is
Another tool must be used to define toolbars, keyboard functions and menus that may be used : the menu painter (transaction SE41).

